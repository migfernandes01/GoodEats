<% layout('layouts/boilerplate.ejs') %>
    <div id="cluster-map"></div>
    <div class="d-flex flex-row">
        <div class="col-md-3">
            <form action="/restaurants/new">
                <button class="btn btn-lg btn-success mt-3">➕ Add a restaurant</button>
            </form>
            <div class="card my-3">
                <div class="row">
                    <div class="">
                        <div class="card-body">
                            <h3>Filters:</h3>
                            <form action="/restaurants" method="get">
                                <%- include('../partials/filter.ejs') %> 
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-md-9 ms-5">
            <h1 class = "my-3">Results:</h1>
            <%- include('../partials/restaurants.ejs') %> 
        </div>   
    </div>
    <script>
        const mapToken = '<%-process.env.MAPBOX_TOKEN%>';   //mapbox token on .env file
        const restaurants = { features: <%-JSON.stringify(restaurants)%> };   //setting up campgrounds data for cluster map
    </script>
    <script src="scripts/clusterMap.js"></script>