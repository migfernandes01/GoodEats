<% layout('layouts/boilerplate.ejs') %>
    <div class="d-flex flex-row">
        <div class="col-md-3">
            <div class="card mt-5">
                <div class="row">
                    <div class="">
                        <div class="card-body">
                            <h3>Filters:</h3>
                            <form action="/users/<%=user[0]._id%>" method="get">
                                <%- include('../partials/filter.ejs') %> 
                            </form>
                        </div>
                    </div>
                </div>
            </div>
            <form action="/restaurants/new">
                <button class="btn btn-lg btn-success my-3">➕ Add a restaurant</button>
            </form>
        </div>
        <div class="col-md-9 ms-5">
            <h1 class = "my-3">Restaurants submitted by <%=user[0].username%>:</h1>
            <%- include('../partials/restaurants.ejs') %>
        </div>
    </div>